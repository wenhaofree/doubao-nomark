[project]
name = "doubao-nomark-worker"
version = "1.0.4"
description = "Cloudflare Worker for Doubao media parsing"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "workers-runtime-sdk",
]
